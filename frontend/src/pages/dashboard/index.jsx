import { Layout } from '@/components/Dashboard';

export default function Dashboard() {
    return (
        <Layout>
            <div className="w-full min-h-screen">dsd</div>
        </Layout>
    );
}
